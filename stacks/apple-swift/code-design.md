# Code design: three rings (Native Apple with Swift and SwiftUI)

UNFILLED — this stack is a scaffold. Every section below must be written from the owner's
conventions before `kartograph-plan` will use it.

## 1. The hexagon in this stack

UNFILLED — what is ring 1 (the driving adapter the person touches), ring 2 (the core),
ring 3 (the driven adapters); how dependencies point inward; how data flows outward.

## 2. Module and package layout

UNFILLED — one capability = which module(s); the sub-packages; where shared types live.

## 3. UI state contract

UNFILLED — the state, event and effect shapes; loading and error; one-shot effects.

## 4. Naming

UNFILLED — a table like the KMP one: thing, name, where, ring.

## 5. Dependency injection and navigation

UNFILLED — the container, the scopes, how bindings change between rings, the router.

## 6. The three rings, in order

### Ring 1: screens

UNFILLED — what is built on fakes with sample data; what done means.

### Ring 2: domain

UNFILLED — use case implementations, ports, rules, tests, in-memory data behind a demo flag.

### Ring 3: adapters

UNFILLED — persistence, HTTP, platform, server; the rebinding.

## 7. Rules that hold in every ring

UNFILLED.
